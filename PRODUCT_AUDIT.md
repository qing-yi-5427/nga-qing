# nga-qing 产品对比与本轮验收

更新日期：2026-08-20

## 对比基线

- nga-just-works：重点参考版块收藏、层级组织、列表刷新和高频操作入口。
- MNGA：重点参考主题列表的信息层级、已读状态、阅读续接、楼层跳转与帖子工具。
- MNGA 仅用于公开 UI/交互调研，未使用其代码或资源；NGA 协议兼容行为曾参考 GPL-2.0 的 nga-just-works，本项目因此采用 GPL-2.0 并单独维护来源台账。

## 本轮已完成

### 应用图标

- 启动图标已替换为用户提供的橙色 `NGA` 翻页图案，并同步生成普通、圆形、自适应及主题单色资源。

### 版块

- 真实版块图标与父子层级。
- 父版块右侧箭头展开/收起，晴风村等子版块保持缩进关系。
- 进入版块后展示“全部 / 精华区”等下一级分类。
- 本地收藏版块；首页提供可折叠快捷区。
- 下拉刷新、全站搜索。

### 主题列表

- 标题统一左对齐，不再为缺失头像预留伪头像列。
- “热门 / 爆”标识移至右侧，回复数也固定在右侧。
- 作者和更新时间降级为辅助信息；使用最后回复时间。
- 已读主题自动变淡。
- 下拉刷新、版内搜索、按最后回复/发帖时间排序。
- 点击标题：列表不在顶部时回到顶部，已在顶部时刷新。
- 版块收藏入口。

### 帖子阅读

- 楼层显示作者头像；兼容 NGA 旧图床、头像历史 JSON、无协议及相对地址，并对当前头像 CDN 的 HTTPS 567 提供严格限域的 HTTP 回退；缺失或加载失败时使用中性用户图标，不显示圆形单字头像。
- 浏览历史、上次阅读楼层记录与恢复。
- 跳转楼层、只看楼主、当前页刷新。
- 楼层改为连续阅读流：主楼标题、楼主标识、楼层号、引用关系和楼中楼的信息层级重新排版。
- “写回复 / 翻页 / 只看作者”收纳进右下角单一圆形快捷入口，并提供页码直达面板。
- 快捷入口在向下阅读时自动收起并隐藏，向上滑动时重新显示。
- 分享链接、浏览器打开。
- 正文图片支持 GIF 动图；加载中显示紧凑进度，失败时显示可点按重试的提示，不再留下大片空白。
- 下拉刷新、标题点击回顶部。
- 清理正文中未支持的 color/size/font 等展示型 BBCode，避免标记原样露出。

### 手势与导航

- Android 系统预测性返回：拖动阶段不切换页面，提交并松手后才返回；取消则留在当前页。
- 页面切换使用约 200ms 的方向性横向推入/推出，不使用淡入淡出；返回后快速恢复列表交互。
- 主题列表、帖子、搜索、设置和历史页支持内容区向右滑动返回。
- 自定义右滑在松手越过阈值后返回，拖动时仅显示轻量位移和返回提示。
- 网页版帖子不再挂载内容区右滑识别器和位移图层，WebView 独占页面触摸；仍保留 Android 左侧边缘的系统返回手势。

### 数据与稳定性

- 设置中可选择 `bbs.nga.cn`、`ngabbs.com`、`nga.178.com` 或旧兼容域名；原生 API、Referer、登录页、网页兜底与分享链接统一跟随选择。
- 域名切换仅改写论坛入口，`img.nga.cn` 等附件/CDN 主机保持原样，避免图片请求被错误导向论坛域名。
- 修复 WebView 把 UID/CID 误写成单条 Cookie 的问题；现在分别写入并等待完成，网页兜底可继承登录状态。
- 修复 NGA 父域访客 UID 与账号 UID 同名冲突：同步时覆盖 `.nga.cn` 父域认证 Cookie，捕获时只接受数字账号 UID；网页兜底禁用旧错误页缓存。
- 数据库从 v1 平滑迁移到 v2，保留原有帖子收藏。
- 新增版块收藏和浏览历史表，不使用破坏性重建。
- 修复深色模式下未指定内容色导致标题变黑的问题。
- 修正版块合集搜索参数，stid 不再误当 fid。
- 修复帖子毫秒时间戳被重复乘 1000，导致所有楼层显示“刚刚”的问题。
- 版块目录保存首个可见项及像素偏移，进入版块再返回时恢复原浏览位置。
- 主题列表保存首个可见项及像素偏移，从帖子返回时恢复进入帖子前的浏览位置；仅在主动切换排序或筛选时回到顶部。

### 性能与流畅度

- Compose 从 1.7.2 升级到 1.9.5，获得新版 Lazy 列表滚动与复用优化；主列表均提供稳定 key 和 contentType。
- 版块图标、作者头像和正文图片按显示尺寸解码；移除 LazyColumn 中开销较高的 SubcomposeAsyncImage 与全局图片淡入。
- 正文、签名、引用和楼中楼 BBCode/HTML 在后台线程一次性解析，界面滚动时不再重复构造正则或解析字符串。
- 版块/主题滚动位置只在滚动停止或页面离开时保存，不再每移动一个像素写入 SavedStateHandle。
- 数据模型标注不可变语义，降低无关重组；Release 开启 R8、资源裁剪和 Baseline Profile/ProfileInstaller。
- 新增 benchmark 模块，覆盖暖启动和论坛列表上下滚动，可持续输出 StartupTiming 与 FrameTiming 指标。
- 应用显式请求当前物理分辨率下的最高刷新率，避免部分系统把第三方应用限制为 60Hz，也不会为追求高刷切换分辨率。
- 网页兜底页仅在创建时清理旧未登录缓存，加载后恢复正常资源缓存，避免 LOAD_NO_CACHE 降低网页图片与脚本复用效率。

## 暂缓项目

以下功能价值较高，但需要确认 NGA 写接口、错误语义和账号安全边界，不适合在本轮以“能点但不可靠”的状态上线：

- 新建主题、图片上传。
- 站内信、回复提醒和系统通知。
- 多账号切换。
- 与 NGA 服务端同步版块收藏、浏览进度。
- 完整复刻任意 BBCode 的字号、颜色和字体；当前优先保证内容干净可读。

## 验收结果

- Gradle 单元测试、Debug Lint、Release/R8 构建和 benchmark APK 构建通过。
- 已在 Android 真机覆盖安装并启动。
- 已验证 v1→v2 数据库迁移、真实版块加载、晴风村分类、版块收藏、帖子打开、浏览历史、已读变淡、连续右滑返回，以及帖子快捷入口的展开、下滑隐藏和上滑恢复。
- 已验证切换到 `ngabbs.com` 后原生首页请求返回 200，且四个论坛域名与图床均同时持有 UID/CID Cookie；验收后恢复为推荐主站 `bbs.nga.cn`。
- 已验证帖子“情侣之间不聊天正常吗？”第 13 楼的 2.5 MB GIF 可在真机正常请求、解码和显示。
- 已在真机直接复测 JSON 失败帖子 `tid=47382575`：网页版可识别当前账号并显示收藏、回复等登录态操作，不再出现“未登录”。
- 性能版已在真机覆盖安装并冷启动成功，系统报告冷启动 TotalTime 256ms；安装包不含 DEBUGGABLE 标志，ProfileInstaller 已成功安装基线描述文件。
- 真机窗口已请求 preferredDisplayMode=1，系统显示 activeMode=1 / activeRenderFrameRate=120Hz，分辨率保持 1200×2608。
- WebView 手势事件链真机验证：纯竖滑从 0 滚至 9310px，带 250px 横向偏移的斜向竖滑继续滚至 36467px，纯横滑不触发应用返回（backCount=0）。
- 启动及上述操作期间未发现 AndroidRuntime、Room 或 SQLite 崩溃。
- MIUI 已开启“USB 调试（安全设置）”，后续可以继续执行真机自动化手势与滚动验证。
- 项目及应用名称已统一为 `nga-qing`，Android `applicationId` 为 `com.qingyi5427.ngaqing`；不兼容旧版 `com.ngaclient.app` 的安装数据，请求中不再使用其他客户端的身份标识。
- 最终 Release APK：`nga-qing-release.apk`，SHA-256：`A86F3AF2663D2A886EBDF6948934521A19AE1CFAC143057A781D03D2BCC29CD9`。

## 参考

- https://github.com/tophtab/nga-just-works
- https://github.com/BugenZhao/MNGA
- 详细来源和许可边界见 `SOURCES.md` 与 `THIRD_PARTY_NOTICES.md`。
