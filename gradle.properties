org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
org.gradle.parallel=true
android.useAndroidX=true
kotlin.code.style=official
android.nonTransitiveRClass=true
android.injected.androidX=true
# NOTE: org.gradle.java.home was removed — it pointed to a Windows-only JDK.
# On macOS, either set JAVA_HOME (JDK 17+) or let Gradle auto-detect.
